{
  "name": "system-awakening",
  "version": "1.0.0",
  "private": true,
  "description": "System Awakening — a LitRPG interactive fiction engine",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "node tests/test_runner.mjs",
    "dev": "echo 'Open index.html in a browser — ES modules work natively with type=module'"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
